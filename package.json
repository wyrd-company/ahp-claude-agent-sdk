{
  "name": "@wyrd-company/ahp-claude-agent-sdk",
  "version": "0.2.0",
  "description": "Agent Host Protocol provider adapter for Claude Agent SDK.",
  "type": "module",
  "license": "MIT",
  "author": "Wyrd Company",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wyrd-company/ahp-claude-agent-sdk.git"
  },
  "files": [
    "dist/src",
    "README.md",
    "LICENSE"
  ],
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepack": "npm run clean && npm run build",
    "prepublishOnly": "npm run verify",
    "test": "node --test --import tsx test/*.test.ts",
    "test:live": "scripts/validate-live-claude-agent-sdk.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.170",
    "@microsoft/agent-host-protocol": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@wyrd-company/ahp-provider-kit": "^0.2.0"
  },
  "peerDependencies": {
    "@wyrd-company/ahp-server": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@wyrd-company/ahp-server": "^0.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
